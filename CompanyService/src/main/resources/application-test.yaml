spring:
  application:
    name: CompanyService
  jpa:
    properties:
      datasource:
        url:
        username:
        password:
      hibernate:
        show_sql: "true"
  data:
    redis:
      database: 0
      connect-timeout: 2000ms
      client-type: lettuce
      lettuce:
        pool:
          max-active: 20
          max-idle: 10
          min-idle: 5
          max-wait: 2000ms

server:
  port: 8081

